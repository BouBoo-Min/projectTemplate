import React from 'react'
import styles from './index.module.less'

const prefixCls = 'home'

const Home = () => {
  return (
    <div className={styles[`${prefixCls}`]}>Home</div>
  )
}

export default Home